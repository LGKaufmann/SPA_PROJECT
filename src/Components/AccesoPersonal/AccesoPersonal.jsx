const AccesoPersonal = () => {
  return (
    <section id="acceso-personal" className="py-16 bg-white">
      <div className="container mx-auto text-center">
        <h2 className="text-3xl font-bold mb-4">
          Acceso para Personal del Spa
        </h2>
        <button className="mt-4 px-8 py-3 bg-blue-500 text-white rounded-lg">
          Iniciar Sesión
        </button>
      </div>
    </section>
  );
};

export default AccesoPersonal;
